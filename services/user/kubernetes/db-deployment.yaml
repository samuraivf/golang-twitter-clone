apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.17.0 (HEAD)
  labels:
    io.kompose.service: db-user
  name: db-user
spec:
  replicas: 1
  strategy: {}
  selector:
    matchLabels:
      io.kompose.service: db-user
  template:
    metadata:
      labels:
        io.kompose.service: db-user
    spec:
      containers:
      - env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: postgres-configmap-user
              key: POSTGRES_DB
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret-user
              key: POSTGRES_PASSWORD
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-secret-user
              key: POSTGRES_USER
        image: postgres:latest
        name: db-user
        ports:
        - containerPort: 5432
        resources: 
          limits:
            cpu: 0.2
            memory: 100M
      restartPolicy: Always
status: {}