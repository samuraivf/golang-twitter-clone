version: "3.8"

services:
  twitter-clone:
    build: ./
    command: ./wait-for-postgres.sh db ./twitter-clone-tag
    ports:
      - 7007:7007
    depends_on:
      - db
  
  db:
    restart: always
    image: postgres:latest
    volumes:
      - ./.database/postgres/data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgreSQL
      - POSTGRES_USER=postgres
      - POSTGRES_DB=postgres
    ports:
      - 5437:5432

volumes:
  db:
    driver: local